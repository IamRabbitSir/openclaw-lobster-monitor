HOST=0.0.0.0
PORT=80
# Optional: if omitted, the server will generate and persist one automatically.
INGEST_TOKEN=replace-with-a-long-random-token
PUBLIC_BASE_URL=http://your-server-ip
SOURCE_TTL_MS=30000
MAX_BODY_BYTES=1048576
